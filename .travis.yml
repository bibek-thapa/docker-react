sudo: required
services :
    - docker 

before_install :
  - docker build -t bibekthapahere/docker-react -f Dockerfile.dev .

script :
  - docker run -e  CI=true bibekthapahere/docker-react npm run test -- --coverage